var pages = function (pages, action) {
    if (pages === void 0) { pages = {}; }
    switch (action.type) {
        case 'REQUEST_TODO_PAGE': return {};
    }
};
pages,
    [action.payload.page];
{
    ids: [],
        fetching;
    true;
}
return pages;
